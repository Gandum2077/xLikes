# xLikes

请生成并维护一个完整的 Node.js + Web 应用：通过 [X（Twitter）官方 API](https://developer.x.com/) 将用户自己点赞的内容同步到本地，并在本地完成浏览、搜索、随机查看、评分、打标签、写备注、归档和媒体查看。

本项目运行在 JSBox 的定制 Node 环境中，必须严格遵守 [AGENTS.md](AGENTS.md) 中的 Node v10.13.0、CommonJS、前后端分离、无构建工具、`node index.js` 启动等约束。任何新增代码都必须兼容这些约束。

---

## 1. 核心原则

- 应用只在本地运行，服务由 Express 提供，前端由同一服务静态托管。
- 前端使用纯 HTML、CSS、JavaScript 和 axios，不使用构建工具。
- 后端使用 Express REST API，统一使用 `/api` 前缀。
- 本地业务数据使用 lowdb 的 `db.json` 保存。
- 登录凭据统一保存在 `.env` 中，不写入 `db.json`。
- 只使用 X 官方 API。
- 所有请求、响应、错误和关键状态变化都应具备可排查的日志，但日志必须对 token、secret、cookie 等敏感字段脱敏。

---

## 2. X API 与认证

### 2.1 支持的认证方式

应用读取用户点赞列表时必须使用 OAuth 2.0 User Context Access Token，或兼容的用户上下文认证。不得用 App-only Bearer Token 调用需要用户上下文的端点。

OAuth 2.0 Token 至少需要以下 scope：

- `tweet.read`
- `users.read`
- `like.read`

如需长期使用，授权时还应包含：

- `offline.access`

### 2.2 凭据配置

`.env` 中保存：

- `X_CLIENT_ID`
- `X_CLIENT_SECRET`
- `X_ACCESS_TOKEN`
- `X_REFRESH_TOKEN`
- `X_TOKEN_EXPIRES_AT`
- `LOG_ENABLED`
- 可选：`LOG_DIR`
- 可选：`HOST`
- 可选：`PORT`

前端设置凭据页面按 X Developer Console 的理解方式排列，只显示一个大区域：

- 区域标题：`OAuth 2.0 Keys`
- 字段顺序：`Client ID`、`Client Secret`、`Access Token`、`Refresh Token`

### 2.3 Token 刷新

- `Refresh Token` 可选填写。
- 当 Access Token 过期或 X API 返回明确的过期/无效 token 错误时，如果存在 `Refresh Token` 和 `Client ID`，后端应自动请求 `/2/oauth2/token` 刷新 Access Token。
- 如果刷新响应包含新的 `refresh_token`，必须同步写回 `.env`，因为 X 的 refresh token 可能轮换。
- 刷新失败时，错误信息应保留 X 返回的关键错误体，便于判断是 token 无效、client 不匹配、授权撤销还是 scope 不足。

### 2.4 当前授权用户识别

- 用户不需要手动填写 User ID。
- 保存并测试凭据时，后端必须调用 `/2/users/me` 自动获取当前授权用户的 User ID、用户名、昵称和头像。
- 测试凭据不能只测试 `/2/users/me`，还要用当前 User ID 调用一次点赞列表端点，确认该 token 确实能读取 liked tweets。

---

## 3. 数据同步

### 3.1 同步模式

状态页提供：

- `增量同步`
- `完全同步`
- `启动时自动增量同步`

完全同步会批量遍历全部可访问点赞列表，可能消耗较多接口额度，点击前必须弹窗确认，防止误触。

### 3.2 增量同步策略

- 增量同步调用 liked tweets 时，每次请求 `max_results=5`。
- 如果读取到本地已存在的推文，增量同步应停止。
- 增量同步必须记录页数、新增数、更新数、是否因为重复停止、每页读取数量、速率限制信息和错误信息。
- 这样做是为了降低 X API 按条计费或按返回量计费时的浪费。

### 3.3 完全同步策略

- 完全同步用于重新遍历用户可访问的全部点赞列表。
- 完全同步可使用较大的分页数量，但必须明确记录同步摘要。
- 如果某条推文已被 X 删除、不可访问或未再次出现在同步结果中，不要从本地删除它。

### 3.4 同步内容

同步阶段只保存推文元数据和文本信息，不主动下载图片或视频文件。

每条推文至少保存：

- 推文 ID
- 正文
- 发布时间
- 作者 ID、昵称、用户名、头像
- 公开指标，如评论、转发、点赞等
- 引用/转发等关联信息
- 媒体元数据，包括图片、视频、GIF 的远程地址、尺寸、时长、预览图、alt text
- 本地扩展字段：评分、标签、备注、归档状态、本地媒体路径、下载状态、更新时间

---

## 4. 媒体下载与展示

### 4.1 按需下载

- 用户浏览到某条推文时，前端通过 IntersectionObserver 触发该推文媒体下载。
- 图片、GIF 和短视频自动下载。
- 长于 30 秒的视频默认不自动下载，只显示下载按钮，用户点击后再下载。
- 下载失败时要能重试。
- 下载的视频必须实时显示下载进度，包括百分比和已下载/总大小；无法获得总大小时也应显示已下载大小。

### 4.2 图片展示规则

- 推文卡片中的图片应尽量完整显示。
- 如果图片长宽比在 `0.5` 到 `2` 之间，应使用保持比例的完整显示。
- 如果长宽比小于 `0.5` 或大于 `2`，才允许裁剪或限制显示区域，避免超长图/超宽图破坏列表布局。
- 竖屏图不能被强制塞进过高的空白容器，也不能出现图片已加载但卡片仍显示大量空白的情况。

### 4.3 图片查看器

- 单击推文中的图片打开图片查看器。
- 查看器背景变暗，图片放大显示。
- 对于超长图或超宽图，查看器区域必须允许滚动查看完整内容。
- 点击背景空白区域关闭查看器。

### 4.4 iOS 滚动约束

iOS Safari/JSBox WebView 的滚动体验必须作为重点兼容对象：

- 列表中出现视频时，继续向下滑动不应自动闪回到视频所在位置。
- 正在加载图片或媒体下载完成时，不应打断 iOS 滚动视图的减速效果。
- 滚动过程中尽量避免整页重绘，媒体状态更新应优先局部更新，并可延后到滚动减速结束后再渲染。
- 图片使用懒加载和异步解码，视频使用适合移动端的内联播放配置，避免自动加载导致滚动锚点异常。

---

## 5. 前端页面与交互

应用主体有三个 Tab 页面：状态页、浏览页、标签页。另有一个搜索页。整体采用 iOS 风格，推文卡片采用接近 X 的视觉风格，支持浅色和深色模式。

### 5.1 通用导航

- 每个主 Tab 页面都有导航栏，标题居中显示。
- 如果用户已经处于状态页、浏览页、标签页之一，再次点击 tabbar 中相同页面，不应重新加载页面，而是让当前滚动视图回到顶部。
- 页面布局要兼容桌面端、iPhone Safari 和 JSBox WebView。

### 5.2 状态页

未登录或凭据不可用时：

- 显示引导，帮助用户去 X Developer Console 创建 Project/App、配置 OAuth 2.0、选择权限、生成用户上下文 token。
- 引导用户填写 `Client ID`、`Client Secret`、`Access Token`、`Refresh Token`。

已登录时：

- 显示当前授权用户头像、昵称、用户名。
- 显示本地推文总数、已归档数、有标签数。
- 提供增量同步、完全同步、启动时自动增量同步。
- 显示 OAuth 2.0 Keys 的保存状态、token 预计过期时间、上次刷新时间、上次刷新失败原因。
- 提供更新凭据和手动刷新 Token。
- 不显示 API 用量查询功能。
- 不显示“登录凭据统一保存在本地 .env 文件，不再写入 db.json。”这类实现说明。

### 5.3 浏览页

- 每次正常浏览加载 50 条，下拉到底自动加载下一页。
- 浏览页导航栏右侧显示“搜索”按钮，靠右排列。
- 提供“随机 10 条”功能；点击后随机显示 10 条本地未归档推文。
- 随机 10 条页面不能再显示“每次加载 50 条”等普通浏览模式说明。
- 浏览页滚动视图顶部不要显示 `class="inline-muted"` 的“随机10条”或“每次加载 50 条”说明。

推文卡片：

- 顶部显示作者头像、昵称、用户名和发布时间。
- 正文下方显示本地标签，标签风格要区别于正文中的 hashtag。
- 标签后显示备注；无标签或备注时不显示对应区域。
- 卡片底部不显示 X 的评论、转发、点赞数量，而显示评分、标签、备注三个操作。
- 评分为 1 到 5，支持清除评分。
- 打标签弹窗包含单行输入框、已有标签联想和最多引用的十个标签快捷项。
- 写备注弹窗使用多行文本框。
- 右上角 ellipsis 菜单提供：归档/取消归档、查看数据、用浏览器打开、用 X App 打开。

### 5.4 标签页

- 列表显示所有已使用标签，每行右侧显示引用数量。
- 排序控件使用 `select` 元素，靠右显示。
- 不显示 `<label for="tagSortSelect">排序</label>`。
- `select` 选项文案为：`按名称排序`、`按数量排序`。
- 点击标签进入搜索页或展示该标签对应推文。

### 5.5 搜索页

搜索页导航栏由返回按钮、搜索输入框、确认按钮组成。

搜索正文支持匹配：

- 推文正文
- 作者昵称
- 用户名
- 本地备注
- `#标签`

搜索参数包括：

- 是否包含已归档推文，默认否
- 最低评分，默认不使用
- 搜索结果排序：
  - 正序，默认
  - 倒序
  - 随机排序
  - 按发布时间正序
  - 按发布时间倒序
- 发布时间起始
- 发布时间结束

搜索参数、最多引用标签、搜索历史等辅助区域：

- 只有进入搜索页面和聚焦搜索文本框时显示。
- 点击确认开始搜索后自动隐藏。

日期选择：

- iOS 上必须能清除已选择的发布时间起始/结束。
- 清除按钮不能导致日期选择器刚打开就自动选中今天并关闭。
- 日期输入与清除按钮不能错位，移动端布局必须稳定。

---

## 6. 日志系统

项目必须具备本地日志系统，方便排查 X API、token、同步和媒体下载问题。

### 6.1 开关与文件

- 通过 `.env` 中的 `LOG_ENABLED=true/false` 控制日志开关。
- 每次应用启动时新建一个日志文件。
- 日志文件名以应用启动时间命名。
- 默认写入 `logs/` 目录。
- 日志同步写入本地文件，同时在命令行输出。
- 不需要为日志系统增加前端 UI。

### 6.2 记录内容

日志应记录：

- 应用启动信息
- API 请求和响应
- X API 请求、响应、错误
- OAuth refresh 请求、响应、错误
- 同步开始、结束、摘要和失败原因
- 媒体下载开始、响应头、进度、完成、失败
- `.env` 更新事件，但不能记录明文敏感值

### 6.3 脱敏

以下字段必须脱敏：

- `Authorization`
- `Cookie`
- `Set-Cookie`
- `Access Token`
- `Refresh Token`
- `Client Secret`
- Bearer token
- 任何 key 名中包含 `token`、`secret`、`password`、`cookie` 的字段

---

## 7. 本地数据与文件

### 7.1 db.json

`db.json` 保存业务状态，不保存登录凭据。

应包含：

- `settings`：非敏感设置，如标签排序方式、搜索历史、当前授权用户信息、自动同步设置等
- `sync`：同步状态、上次同步摘要、错误信息、速率限制信息
- `tweets`：推文本地数据

### 7.2 .env

`.env` 保存敏感凭据和运行配置，不提交到版本库。

### 7.3 媒体文件

- 媒体文件保存到前端可访问的本地目录，例如 `public/media/`。
- 文件名应包含 tweet ID 和 media key，并清理非法字符。
- 本地路径写回对应推文媒体对象。

---

## 8. 错误处理

后端所有 API 必须有统一错误处理。

需要重点识别并给出清晰中文提示：

- 未填写 Access Token
- 使用了 App-only Bearer Token 调用用户点赞列表
- Access Token 过期或无效
- Refresh Token 无效、过期、已轮换或与 Client 不匹配
- scope 不足
- X API 余额、额度或速率限制不足
- 网络错误
- 媒体没有可下载地址
- 本地文件写入失败

错误响应应保留必要的 `detail` 字段，便于从日志分析真实原因。

---

## 9. JSBox 特殊要求

### 9.1 启动前端

`index.js` 末尾必须保留 AGENTS.md 中指定的 JSBox 启动代码。运行方式必须是：

```bash
node index.js
```

### 9.2 打开 X App

Node 初始化时需要识别是否运行在 JSBox 中。跳转 X App 时，JSBox 环境应使用 JSBox API 打开 URL scheme；普通浏览器环境使用常规链接。

示例：

```javascript
var isJsbox = typeof $jsbox !== "undefined";
var app = require("app");

app.openURL("twitter://status?id=2050938561504112755");
```

### 9.3 移动端限制

- 不依赖后台运行。
- 不使用 `child_process`。
- 不依赖系统服务。
- 不使用需要原生扩展的依赖。
- 不使用 Node v10 不支持的语法或 API。

---

## 10. 验收要求

实现或修改本项目时，至少应确认：

- `node index.js` 可以启动。
- 前端页面由 Express 正常提供。
- 保存并测试凭据会自动获取当前授权用户 ID。
- 增量同步每次读取 5 条，并在遇到本地重复推文后停止。
- 完全同步点击前有确认弹窗。
- Access Token 过期时会尝试自动刷新；刷新成功后 `.env` 被更新。
- 刷新失败时日志能看到 X 返回的错误体，并且敏感字段已脱敏。
- 随机功能一次显示 10 条。
- 搜索支持排序、发布时间区间、最低评分和是否包含归档。
- 图片按长宽比正确显示，点击可打开图片查看器。
- 视频和图片下载进度能实时或准实时显示。
- iOS 滚动不会因视频、图片加载或媒体状态更新而跳回旧位置或中止减速。
- 标签页排序控件文案和布局符合要求。
- 状态页不显示 API 用量查询，也不显示 App-only Bearer Token 相关设置。
